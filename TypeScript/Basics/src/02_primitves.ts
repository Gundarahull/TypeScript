// Number, String, Boolean, BigInt, Symbol,

// Primitve DataTypes
const num: number = 900;

const username: string = "looser";

const bigi: bigint = 2n;

console.log("bigi-----------", bigi);

const token: unique symbol = Symbol("TOKEN");

// 30 minutes
